<!DOCTYPE html>
<html>
<body>

<h1>Link for your new class is generated</h1>
<% if (typeof newClassLink !== 'undefined') {%>
    <p>For class <%=newClass.className%> </p>

    <p>This is your unique link for the class you just created. You can share this link with your students: <%= classArr[classArr.length-1].classes_id%>
        <a href=<%= newClassLink %>?classId=<%= newClassId%>><%= newClassLink %>?classId=<%= newClassId%></a>

    </p>
    <h2>Here are all your registered classes: </h2>
    <div>
        <ul>
            <% for (var i = 0; i < classArr.length ; i++){ %>
                <li>Emoji Sharing unique link for class id <%= classArr[i].classes_id %>
                    <a href=<%= path+classArr[i].id %>?classId=<%= classArr[i].classes_id%>><%= path+classArr[i].id %>?classId=<%= classArr[i].classes_id%></a>
                </li>
            <% }%>
        </ul>

    </div>
<%}else if (typeof newClassId !== 'undefined') {%>
    <a href=<%= path+classArr[classArr.length-1].id %>?classId=<%= classArr[classArr.length-1].classes_id%> %>>Go to Emoji Sharing App!</a>
<%}%>

<h2>Generate another class link? </h2>
<div>
    <a href="/instructor">Click here to go back.</a>
</div>

</body>
</html>
