<!DOCTYPE html>
<html>
<head>
</head>
<body>

    <form id="studentForm" class="form" action="/getClassLink" method="POST">
        <% if (typeof classObj === "undefined") {%>
            <h1>Look up your class link with by entering your class id :</h1>

            <p>Enter class id for the classes you have registered in: <p>
            <div class="form-group">
                <label for="classId" class="text-info">Class Id:</label><br />
                <input type="classId" name="classId" id="classId" class="form-control" required>
            </div>
            <div class="form-group">
                <input type="submit" name="submit" class="btn btn-info btn-md" value="Show Class Link"/>
            </div>
        <%}else {%>
            <h1>Here are all your registered classes:</h1>
            <div>
                <ul>
                    <% for (var i = 0; i < classObj.length ; i++){ %>
                        <li class="class-list"><a target="_blank" href=<%= path+classObj[i].id%>?classId=<%= classObj[i].classes_id%>>Go to Emoji Sharing app for
                                class link id: <%= classObj[i].id %>, class id: <%=classObj[i].classes_id%> <%=classObj[i].class_name%> on <%=classObj[i].datetime%> </a>
                        </li>
                    <% }%>
                </ul>

            </div>
        <%}%>
    </form>
</body>
</html>
